---
description: Sync shared rules from CLAUDE.md to AGENTS.md, preserving tool-specific sections.
allowed-tools: Bash(bash *)
---

Run the sync-rules script to push CLAUDE.md shared rules into AGENTS.md.

```
bash scripts/sync-rules.sh to-codex
```

On Windows, run via Git Bash or WSL.

Report the result.
